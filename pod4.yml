apiVersion: v1
kind: Pod
metadata:
  name: snehal-pod1
spec:
  containers:
  - name: snehal-pod1
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
