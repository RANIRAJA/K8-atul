ersion: v1
kind: ReplicationController
metadata:
  name: 
spec:
  replicas: <Replicas>
  selecto app: myapp
  templat metadata:
      name: myapp
      label     app: myapp
    spec:
      containers:
        - name: myapp
          image: <Image>
          ports:
            - containerPort: <Port>
